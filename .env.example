# Home Assistant connection
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your_long_lived_access_token_here

# Optional: path to HA config directory (for git versioning)
# On HA OS / Supervised: /config  (via SSH or mapped volume)
# Locally (dev): leave empty to skip git features
HA_CONFIG_PATH=/config
